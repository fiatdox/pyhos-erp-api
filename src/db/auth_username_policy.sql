-- ══════════════════════════════════════════════════════════════════════════════
-- นโยบายชื่อผู้ใช้ — กรณี username เป็น "เลขบัตรประชาชน"
--
-- ที่มา: หน้าเพิ่มบุคลากร (hr/users) ตั้ง username = password = เลขบัตรให้อัตโนมัติ
--        ทำให้ผู้ใช้ 1,305 จาก 1,319 คน (98.9%) ใช้เลขบัตรเป็นชื่อผู้ใช้
--        = เปิดเผยเลขบัตรครึ่งหนึ่งของข้อมูลที่ใช้ login และเดาบัญชีคนอื่นได้ง่าย
--
-- ปิดอยู่โดยปริยาย (mode = off) และตั้งต้นที่ scope = pilot (ไม่มีรายชื่อ = ไม่มีผลกับใคร)
-- เพื่อทดสอบกับผู้ใช้ไม่กี่คนก่อน แล้วค่อยขยายเป็น all
-- ══════════════════════════════════════════════════════════════════════════════

INSERT INTO auth_settings (name, value, description) VALUES
    ('username_policy_mode',  'off',
     'off = ไม่ตรวจ | warn = แจ้งเตือนแต่ใช้งานได้ | force = ต้องตั้งชื่อผู้ใช้ใหม่ก่อนใช้งานต่อ'),
    ('username_policy_scope', 'pilot',
     'all = ทุกคนที่เข้าเงื่อนไข | pilot = เฉพาะรายชื่อใน username_policy_pilot'),
    ('username_policy_pilot', '',
     'รายชื่อ username สำหรับนำร่อง คั่นด้วยจุลภาค — ใช้เมื่อ scope = pilot')
ON CONFLICT (name) DO NOTHING;
