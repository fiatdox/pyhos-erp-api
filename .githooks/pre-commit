#!/bin/sh
# เพิ่มเลขเวอร์ชันใน package.json อัตโนมัติก่อน commit
# เปิดใช้ด้วย: git config core.hooksPath .githooks   (ทำครั้งเดียวต่อ clone)
#
# ถ้า node ไม่มีในเครื่อง ปล่อยผ่าน ไม่ขวางการ commit
command -v node >/dev/null 2>&1 || exit 0
node scripts/bump-version.mjs || exit 0
